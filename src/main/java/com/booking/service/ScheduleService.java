package com.booking.service;

import com.booking.payload.ScheduleDTO;

public interface ScheduleService {
    ScheduleDTO createSchedule(ScheduleDTO scheduleDTO);
    // Other methods related to schedule operations
}
